#!/usr/bin/env ruby

class SmashingBoxer
  def initialize(opts = {})
    ENV['SMASHING_BOXER_PATH'] = File.join(__dir__, '../')
  end

  def run
    Kernel.exec("ansible-playbook -i test_hosts site.yml")
  end
end

SmashingBoxer.new.run
