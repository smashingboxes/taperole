check process unicorn with pidfile {{ be_app_path }}/tmp/unicorn.pid
  start program "/etc/init.d/unicorn_{{ app_name }} start"
  restart program "/etc/init.d/unicorn_{{ app_name }} restart"
  stop program "/etc/init.d/unicorn_{{ app_name }} stop"
  if 5 restarts within 5 cycles then timeout
